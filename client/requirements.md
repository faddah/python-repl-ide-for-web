## Packages
@monaco-editor/react | Code editor component
react-resizable-panels | To create the IDE layout with draggable sashes
framer-motion | Smooth animations for UI elements
lucide-react | Beautiful icons

## Notes
Integration with Pyodide requires loading the script from CDN.
The app needs to handle file system syncing with Pyodide's virtual FS.
Dark mode is default for the IDE aesthetic.
